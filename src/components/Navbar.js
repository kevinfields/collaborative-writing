import React from 'react'

const Navbar = () => {
  return (
    <div>
      {/* later on I'll add this as a permanent navbar on the side of the screen, 
          oriented vertically with animations when hovering it and all that material ui 
          stuff that it needs.
      */}
    </div>
  )
}

export default Navbar